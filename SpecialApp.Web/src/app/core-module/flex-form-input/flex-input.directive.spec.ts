import { FlexInputDirective } from './flex-input.directive';

describe('FlexInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexInputDirective();
    expect(directive).toBeTruthy();
  });
});
